function runMore(distance: number): number {
  return distance + 10;
}

console.log(runMore(20));

// function runMore(distance: number): number (선언부)
//    function [함수01]
//    런모어 : 이름
//    (디스텐스 : 변수
//        : 넘버 : 타입
//    ) [인자 02]
//    : 뒤 넘버 -출력값의 타입 [출력03]
//  return distance + 10;
